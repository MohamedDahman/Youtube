package model;

public class Summary {

    public void getResult() {

    /*    CountriesFiles countriesFiles = new CountriesFiles();
        Map countryMap = countriesFiles.countryData();
        List<Video> allVideoTogether = null;
        for (Object countries : countryMap.keySet()) {
            System.out.println("This Part Belong to " + countries.toString());
            System.out.println("----------------------"+countries.toString()+"-----------------");
            Parser parser = new Parser();
            List<Video> allVideo = parser.getDataFromFile(countryMap.get(countries).toString(), countries.toString());
            //allVideo.forEach(e->allVideoTogether.add(e));
*/

/*
            List<String> mostChanelHaveVideoAsList = getMostChanelHaveVideoAsList(allVideo, 5, countries.toString());
            mostChanelHaveVideoAsList
                    .forEach(e -> System.out.println(e));
            System.out.println("------------------------------------------------------");
            System.out.println("---------------------Gregory Part---------------------");
            getCountVideoByCategory(allVideo, 5, countries.toString());
            System.out.println("------------------------------------------------------");

            getVideoByMoreView(allVideo,5,countries.toString());
            System.out.println("------------------------------------------------------");

        }
        System.out.println("------------------this section All Country together---------------------------");
        //getCountVideoByCategoryAllCountry(allVideoTogether,5);
*/

    }


}